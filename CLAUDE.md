# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

`quant_agent` is a quantitative trading/analysis agent project. This file will be updated as the codebase grows.

## Setup

_Add setup instructions here (e.g., `pip install -r requirements.txt`, virtual env creation, `.env` configuration)._

## Commands

_Add commands here as they are established, for example:_

- **Run**: `python main.py`
- **Test**: `pytest`
- **Lint**: `ruff check .` / `mypy .`

## Architecture

_Document the high-level architecture here once it takes shape._
